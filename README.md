## Digital photocard binder with uploads, sticker editing, drag-and-drop ordering, and binder customization

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
